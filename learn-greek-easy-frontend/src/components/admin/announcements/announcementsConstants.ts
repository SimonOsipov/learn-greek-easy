// Threshold (percent) at or above which avg read rate is considered "healthy".
export const AVG_READ_RATE_HEALTHY_THRESHOLD = 20;

// Page size used when the announcements tab fetches the full list client-side
// for in-memory filtering/sorting.
export const ANNOUNCEMENTS_CLIENT_FETCH_PAGE_SIZE = 100;
