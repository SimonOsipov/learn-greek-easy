/**
 * SIT-06 stub — filled in SIT-07b.
 */
import type { SituationDetailResponse } from '@/types/situation';

interface Props {
  situation: SituationDetailResponse;
}

export function SituationDrawerDescription({ situation: _situation }: Props) {
  return null;
}
