/**
 * SIT-06 stub — filled in SIT-07e.
 */
import type { SituationDetailResponse } from '@/types/situation';

interface Props {
  situation: SituationDetailResponse;
}

export function SituationDrawerLinkedNews({ situation: _situation }: Props) {
  return null;
}
