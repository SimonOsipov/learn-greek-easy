/**
 * SIT-06 stub — filled in SIT-07c.
 */
import type { SituationDetailResponse } from '@/types/situation';

interface Props {
  situation: SituationDetailResponse;
}

export function SituationDrawerPicture({ situation: _situation }: Props) {
  return null;
}
