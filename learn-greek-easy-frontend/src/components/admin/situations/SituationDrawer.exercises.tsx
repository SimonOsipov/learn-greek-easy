/**
 * SIT-06 stub — filled in SIT-07d.
 */
import type { SituationDetailResponse } from '@/types/situation';

interface Props {
  situation: SituationDetailResponse;
}

export function SituationDrawerExercises({ situation: _situation }: Props) {
  return null;
}
