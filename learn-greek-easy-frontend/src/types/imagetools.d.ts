/**
 * Type stub for vite-imagetools query-string imports used in Hero.tsx.
 *
 * TypeScript cannot resolve query-string asset imports through path aliases
 * in bundler mode. We solve this by mapping each exact query-string variant
 * to this stub via tsconfig.app.json "paths" entries.
 *
 * Each import returns a `string`: either an srcset attribute value (&as=srcset)
 * or a hashed URL (single-variant imports).
 */
declare const _default: string;
export default _default;
