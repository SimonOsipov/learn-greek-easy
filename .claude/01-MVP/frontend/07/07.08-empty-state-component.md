# Task 07.08: Create Reusable Empty State Component

**Status**: 📋 **PLANNED** (OPTIONAL)
**Parent Task**: [Task 07 - UI Components](../07-ui-components.md)
**Track**: B - Refactoring (Optional)
**Priority**: Low
**Estimated Duration**: 45 minutes
**Dependencies**: None
**Deliverable**: Reusable EmptyState component

---

## Objective

Create a reusable EmptyState component to standardize "no data" displays across the application, replacing inline empty state implementations.

---

## Current Empty State Patterns

**Locations with empty states**:
1. DecksPage - No decks created
2. DeckDetailPage - No flashcards in deck
3. Dashboard - No activity feed items
4. Statistics - No review data
5. Search results - No results found

**Current implementations**: Inline, inconsistent styling, duplicated code

---

## EmptyState Component

**File**: `/src/components/feedback/EmptyState.tsx`

**Interface**:
```typescript
interface EmptyStateProps {
  icon?: React.ComponentType<{ className?: string }>;
  title: string;
  description?: string;
  action?: {
    label: string;
    onClick: () => void;
    variant?: 'default' | 'outline';
  };
  className?: string;
}
```

**Implementation**:
```typescript
import { InboxIcon } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { cn } from '@/lib/utils';

export function EmptyState({
  icon: Icon = InboxIcon,
  title,
  description,
  action,
  className
}: EmptyStateProps) {
  return (
    <div className={cn(
      'flex flex-col items-center justify-center min-h-[400px] text-center p-8',
      className
    )}>
      <Icon className="h-16 w-16 text-muted/50 mb-4" />
      <h3 className="text-lg font-semibold text-gray-900 mb-2">{title}</h3>
      {description && (
        <p className="text-muted max-w-sm mb-6">{description}</p>
      )}
      {action && (
        <Button
          onClick={action.onClick}
          variant={action.variant || 'default'}
        >
          {action.label}
        </Button>
      )}
    </div>
  );
}
```

**Usage Examples**:
```tsx
import { EmptyState } from '@/components/feedback/EmptyState';
import { BookOpenIcon, SearchIcon, ActivityIcon, BarChartIcon } from 'lucide-react';

// No decks
<EmptyState
  icon={BookOpenIcon}
  title="No decks yet"
  description="Create your first deck to start learning Greek words."
  action={{ label: 'Create Deck', onClick: () => setShowCreateDialog(true) }}
/>

// No search results
<EmptyState
  icon={SearchIcon}
  title="No results found"
  description={`No decks match "${searchQuery}". Try a different search term.`}
/>

// No activity
<EmptyState
  icon={ActivityIcon}
  title="No recent activity"
  description="Your learning activity will appear here once you start reviewing flashcards."
/>

// No statistics
<EmptyState
  icon={BarChartIcon}
  title="No statistics yet"
  description="Review some flashcards to start tracking your progress."
  action={{ label: 'Start Reviewing', onClick: () => navigate('/decks') }}
/>
```

---

## Implementation Steps

1. Create `/src/components/feedback/EmptyState.tsx` (15 min)
2. Replace empty states in DecksPage (10 min)
3. Replace empty states in Dashboard (10 min)
4. Replace empty states in Statistics (10 min)

---

## Success Criteria

- [ ] EmptyState component created
- [ ] Consistent styling across all empty states
- [ ] Icon, title, description, and action button configurable
- [ ] Accessible (proper heading hierarchy, focus management)
- [ ] Responsive (works on mobile and desktop)
- [ ] Documented in Components-Reference.md

---

## Time Breakdown

| Activity | Duration |
|----------|----------|
| Create EmptyState component | 15 min |
| Replace DecksPage empty state | 10 min |
| Replace Dashboard empty state | 10 min |
| Replace Statistics empty state | 10 min |
| **TOTAL** | **45 min** |
