# Task 03.01: Design Authentication Pages UI

**Task ID**: 03.01
**Task Name**: Design Authentication Pages UI
**Status**: ✅ Completed
**Created**: 2025-10-28
**Completed**: 2025-10-28
**Time Estimate**: 45 minutes
**Actual Time**: 45 minutes
**Priority**: High - First subtask of Task 03
**Dependencies**: Task 02 (Core Frontend Setup) ✅ Completed

---

## Overview

This task focuses on creating the visual UI structure for authentication pages without implementing any functional logic. We'll design and build three authentication pages (Login, Registration, and Password Reset placeholder) with proper layout, styling, and visual components. The forms will be visually complete but non-functional - actual validation and submission logic will be implemented in tasks 03.03 and 03.04.

### What This Task Accomplishes
- Creates the complete visual layout for authentication pages
- Establishes consistent authentication page design patterns
- Provides UI foundation for later functional implementation
- Ensures responsive design across all device sizes
- Sets up reusable authentication layout wrapper

### How It Fits Into Task 03
This is the first subtask that establishes the visual foundation. Subsequent tasks will:
- 03.02: Add state management (Zustand)
- 03.03: Implement login functionality and validation
- 03.04: Implement registration functionality and validation
- 03.05: Add protected routes
- 03.06-03.08: Complete remaining authentication features

### Dependencies
- ✅ React project initialized (Task 02.01)
- ✅ Tailwind CSS configured with custom theme (Task 02.02)
- ✅ Shadcn/ui base components installed (Task 02.03)
- ✅ Project file structure established (Task 02.04)
- ✅ Base layout components created (Task 02.08)

---

## Pages to Create

### 1. Login Page (`/login`)
**File**: `src/pages/auth/Login.tsx`

**Visual Elements**:
- Centered card container (max-width: 400px)
- Greek welcome text: "Καλώς ήρθατε!" (Welcome!)
- Email input field with label
- Password input field with visibility toggle icon
- "Remember me" checkbox
- Primary login button with gradient
- "Forgot password?" link
- Divider with "Or continue with"
- Google OAuth button (disabled state)
- "Sign up for free" link at bottom

### 2. Registration Page (`/register`)
**File**: `src/pages/auth/Register.tsx`

**Visual Elements**:
- Centered card container (max-width: 400px)
- "Create your account" heading
- Full name input field
- Email input field
- Password input with strength indicator bar
- Confirm password input
- "I agree to terms & conditions" checkbox
- Primary register button with gradient
- Divider with "Or continue with"
- Google OAuth button (disabled state)
- "Already have an account? Sign in" link

### 3. Password Reset Placeholder (`/forgot-password`)
**File**: `src/pages/auth/ForgotPassword.tsx`

**Visual Elements**:
- Centered card container
- "Password Reset" heading
- "Coming in Phase 2" message
- Informative text about future functionality
- "Back to login" button/link
- Icon or illustration (optional)

### 4. Shared Authentication Layout
**File**: `src/components/auth/AuthLayout.tsx`

**Purpose**: Wrapper component for consistent auth page styling
- Background gradient (blue-50 to purple-50)
- Min-height screen centering
- Responsive padding
- Optional logo/brand placement

---

## Implementation Steps

### Step 1: Install Required Shadcn/ui Components (5 minutes)

```bash
# Navigate to frontend directory
cd /Users/samosipov/Downloads/learn-greek-easy/learn-greek-easy-frontend

# Install missing Shadcn components for auth pages
npx shadcn-ui@latest add input
npx shadcn-ui@latest add label
npx shadcn-ui@latest add checkbox
```

### Step 2: Create Authentication Layout Wrapper (5 minutes)

**File**: `src/components/auth/AuthLayout.tsx`

```typescript
import React from 'react';

interface AuthLayoutProps {
  children: React.ReactNode;
}

export const AuthLayout: React.FC<AuthLayoutProps> = ({ children }) => {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-50 via-white to-purple-50 px-4 py-8">
      <div className="w-full max-w-md">
        {children}
      </div>
    </div>
  );
};
```

### Step 3: Create Login Page UI (10 minutes)

**File**: `src/pages/auth/Login.tsx`

```typescript
import React, { useState } from 'react';
import { Link } from 'react-router-dom';
import { Eye, EyeOff } from 'lucide-react';
import { AuthLayout } from '@/components/auth/AuthLayout';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Checkbox } from '@/components/ui/checkbox';

export const Login: React.FC = () => {
  const [showPassword, setShowPassword] = useState(false);

  return (
    <AuthLayout>
      <Card className="shadow-xl">
        <CardHeader className="space-y-1 text-center">
          <div className="mb-4">
            <span className="text-4xl">🏛️</span>
          </div>
          <CardTitle className="text-2xl font-bold">
            Καλώς ήρθατε!
          </CardTitle>
          <CardDescription>
            Welcome back! Sign in to continue learning Greek
          </CardDescription>
        </CardHeader>

        <form onSubmit={(e) => e.preventDefault()}>
          <CardContent className="space-y-4">
            <div className="space-y-2">
              <Label htmlFor="email">Email</Label>
              <Input
                id="email"
                type="email"
                placeholder="your@email.com"
                autoComplete="email"
              />
            </div>

            <div className="space-y-2">
              <Label htmlFor="password">Password</Label>
              <div className="relative">
                <Input
                  id="password"
                  type={showPassword ? 'text' : 'password'}
                  placeholder="Enter your password"
                  autoComplete="current-password"
                />
                <Button
                  type="button"
                  variant="ghost"
                  size="icon"
                  className="absolute right-2 top-1/2 -translate-y-1/2 h-8 w-8"
                  onClick={() => setShowPassword(!showPassword)}
                >
                  {showPassword ? <EyeOff size={16} /> : <Eye size={16} />}
                </Button>
              </div>
            </div>

            <div className="flex items-center justify-between">
              <div className="flex items-center space-x-2">
                <Checkbox id="remember" />
                <Label
                  htmlFor="remember"
                  className="text-sm cursor-pointer font-normal"
                >
                  Remember me
                </Label>
              </div>
              <Link
                to="/forgot-password"
                className="text-sm text-primary hover:underline"
              >
                Forgot password?
              </Link>
            </div>
          </CardContent>

          <CardFooter className="flex flex-col space-y-4">
            <Button
              type="submit"
              className="w-full bg-gradient-to-br from-[#667eea] to-[#764ba2] hover:opacity-90 text-white"
              size="lg"
            >
              Sign In
            </Button>

            <div className="relative w-full">
              <div className="absolute inset-0 flex items-center">
                <span className="w-full border-t" />
              </div>
              <div className="relative flex justify-center text-xs uppercase">
                <span className="bg-background px-2 text-muted-foreground">
                  Or continue with
                </span>
              </div>
            </div>

            <Button
              type="button"
              variant="outline"
              className="w-full"
              disabled
            >
              <svg className="mr-2 h-4 w-4" viewBox="0 0 24 24">
                <path
                  d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"
                  fill="#4285F4"
                />
                <path
                  d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"
                  fill="#34A853"
                />
                <path
                  d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"
                  fill="#FBBC05"
                />
                <path
                  d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"
                  fill="#EA4335"
                />
              </svg>
              Google (Coming Soon)
            </Button>

            <p className="text-center text-sm text-muted-foreground">
              Don't have an account?{' '}
              <Link to="/register" className="text-primary hover:underline font-medium">
                Sign up for free
              </Link>
            </p>
          </CardFooter>
        </form>
      </Card>
    </AuthLayout>
  );
};
```

### Step 4: Create Registration Page UI (10 minutes)

**File**: `src/pages/auth/Register.tsx`

```typescript
import React, { useState } from 'react';
import { Link } from 'react-router-dom';
import { Eye, EyeOff } from 'lucide-react';
import { AuthLayout } from '@/components/auth/AuthLayout';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Checkbox } from '@/components/ui/checkbox';
import { Progress } from '@/components/ui/progress';

export const Register: React.FC = () => {
  const [showPassword, setShowPassword] = useState(false);
  const [showConfirmPassword, setShowConfirmPassword] = useState(false);
  const [password, setPassword] = useState('');

  // Simple password strength calculation for UI demo
  const calculatePasswordStrength = (pwd: string): number => {
    if (!pwd) return 0;
    let strength = 0;
    if (pwd.length >= 8) strength += 25;
    if (pwd.length >= 12) strength += 25;
    if (/[A-Z]/.test(pwd) && /[a-z]/.test(pwd)) strength += 25;
    if (/[0-9]/.test(pwd)) strength += 12.5;
    if (/[^A-Za-z0-9]/.test(pwd)) strength += 12.5;
    return Math.min(strength, 100);
  };

  const passwordStrength = calculatePasswordStrength(password);
  const getStrengthColor = () => {
    if (passwordStrength < 33) return 'bg-red-500';
    if (passwordStrength < 66) return 'bg-yellow-500';
    return 'bg-green-500';
  };
  const getStrengthText = () => {
    if (!password) return '';
    if (passwordStrength < 33) return 'Weak';
    if (passwordStrength < 66) return 'Fair';
    return 'Strong';
  };

  return (
    <AuthLayout>
      <Card className="shadow-xl">
        <CardHeader className="space-y-1 text-center">
          <div className="mb-4">
            <span className="text-4xl">📚</span>
          </div>
          <CardTitle className="text-2xl font-bold">
            Create your account
          </CardTitle>
          <CardDescription>
            Start your Greek learning journey today
          </CardDescription>
        </CardHeader>

        <form onSubmit={(e) => e.preventDefault()}>
          <CardContent className="space-y-4">
            <div className="space-y-2">
              <Label htmlFor="name">Full Name</Label>
              <Input
                id="name"
                type="text"
                placeholder="John Smith"
                autoComplete="name"
              />
            </div>

            <div className="space-y-2">
              <Label htmlFor="email">Email</Label>
              <Input
                id="email"
                type="email"
                placeholder="your@email.com"
                autoComplete="email"
              />
            </div>

            <div className="space-y-2">
              <Label htmlFor="password">Password</Label>
              <div className="relative">
                <Input
                  id="password"
                  type={showPassword ? 'text' : 'password'}
                  placeholder="Create a strong password"
                  autoComplete="new-password"
                  value={password}
                  onChange={(e) => setPassword(e.target.value)}
                />
                <Button
                  type="button"
                  variant="ghost"
                  size="icon"
                  className="absolute right-2 top-1/2 -translate-y-1/2 h-8 w-8"
                  onClick={() => setShowPassword(!showPassword)}
                >
                  {showPassword ? <EyeOff size={16} /> : <Eye size={16} />}
                </Button>
              </div>
              {password && (
                <div className="space-y-1">
                  <Progress
                    value={passwordStrength}
                    className="h-1.5"
                  />
                  <p className="text-xs text-muted-foreground">
                    Password strength: <span className={`font-medium ${
                      passwordStrength < 33 ? 'text-red-500' :
                      passwordStrength < 66 ? 'text-yellow-500' :
                      'text-green-500'
                    }`}>{getStrengthText()}</span>
                  </p>
                </div>
              )}
            </div>

            <div className="space-y-2">
              <Label htmlFor="confirmPassword">Confirm Password</Label>
              <div className="relative">
                <Input
                  id="confirmPassword"
                  type={showConfirmPassword ? 'text' : 'password'}
                  placeholder="Confirm your password"
                  autoComplete="new-password"
                />
                <Button
                  type="button"
                  variant="ghost"
                  size="icon"
                  className="absolute right-2 top-1/2 -translate-y-1/2 h-8 w-8"
                  onClick={() => setShowConfirmPassword(!showConfirmPassword)}
                >
                  {showConfirmPassword ? <EyeOff size={16} /> : <Eye size={16} />}
                </Button>
              </div>
            </div>

            <div className="flex items-center space-x-2">
              <Checkbox id="terms" />
              <Label
                htmlFor="terms"
                className="text-sm cursor-pointer font-normal"
              >
                I agree to the{' '}
                <Link to="/terms" className="text-primary hover:underline">
                  terms and conditions
                </Link>
              </Label>
            </div>
          </CardContent>

          <CardFooter className="flex flex-col space-y-4">
            <Button
              type="submit"
              className="w-full bg-gradient-to-br from-[#667eea] to-[#764ba2] hover:opacity-90 text-white"
              size="lg"
            >
              Create Account
            </Button>

            <div className="relative w-full">
              <div className="absolute inset-0 flex items-center">
                <span className="w-full border-t" />
              </div>
              <div className="relative flex justify-center text-xs uppercase">
                <span className="bg-background px-2 text-muted-foreground">
                  Or continue with
                </span>
              </div>
            </div>

            <Button
              type="button"
              variant="outline"
              className="w-full"
              disabled
            >
              <svg className="mr-2 h-4 w-4" viewBox="0 0 24 24">
                <path
                  d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"
                  fill="#4285F4"
                />
                <path
                  d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"
                  fill="#34A853"
                />
                <path
                  d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"
                  fill="#FBBC05"
                />
                <path
                  d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"
                  fill="#EA4335"
                />
              </svg>
              Google (Coming Soon)
            </Button>

            <p className="text-center text-sm text-muted-foreground">
              Already have an account?{' '}
              <Link to="/login" className="text-primary hover:underline font-medium">
                Sign in
              </Link>
            </p>
          </CardFooter>
        </form>
      </Card>
    </AuthLayout>
  );
};
```

### Step 5: Create Password Reset Placeholder Page (5 minutes)

**File**: `src/pages/auth/ForgotPassword.tsx`

```typescript
import React from 'react';
import { Link } from 'react-router-dom';
import { ArrowLeft, Lock } from 'lucide-react';
import { AuthLayout } from '@/components/auth/AuthLayout';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export const ForgotPassword: React.FC = () => {
  return (
    <AuthLayout>
      <Card className="shadow-xl">
        <CardHeader className="space-y-1 text-center">
          <div className="mb-4 mx-auto">
            <div className="w-16 h-16 bg-gradient-to-br from-blue-100 to-purple-100 rounded-full flex items-center justify-center">
              <Lock className="w-8 h-8 text-primary" />
            </div>
          </div>
          <CardTitle className="text-2xl font-bold">
            Password Reset
          </CardTitle>
          <CardDescription>
            This feature is coming in Phase 2
          </CardDescription>
        </CardHeader>

        <CardContent className="space-y-4">
          <div className="text-center space-y-2">
            <p className="text-muted-foreground">
              Password reset functionality will be available soon.
            </p>
            <p className="text-sm text-muted-foreground">
              In the meantime, please contact support if you need help accessing your account.
            </p>
          </div>

          <div className="pt-4">
            <Button
              asChild
              variant="outline"
              className="w-full"
            >
              <Link to="/login">
                <ArrowLeft className="mr-2 h-4 w-4" />
                Back to Login
              </Link>
            </Button>
          </div>
        </CardContent>
      </Card>
    </AuthLayout>
  );
};
```

### Step 6: Update App.tsx Routes (5 minutes)

Add the authentication routes to the main App component:

```typescript
// In src/App.tsx, add these imports:
import { Login } from '@/pages/auth/Login';
import { Register } from '@/pages/auth/Register';
import { ForgotPassword } from '@/pages/auth/ForgotPassword';

// Add these routes in the Routes component:
<Route path="/login" element={<Login />} />
<Route path="/register" element={<Register />} />
<Route path="/forgot-password" element={<ForgotPassword />} />
```

### Step 7: Create Auth Pages Directory Structure (2 minutes)

```bash
# Create auth pages directory
mkdir -p src/pages/auth
mkdir -p src/components/auth

# Move/create files in proper locations
```

### Step 8: Test and Verify UI (3 minutes)

1. Start the development server
2. Navigate to each authentication page
3. Verify responsive design on mobile/tablet/desktop
4. Check all visual elements are displayed correctly
5. Ensure hover states and interactions work
6. Verify no console errors

---

## Design Specifications

### Colors (from Style Guide)
- **Primary Gradient**: `from-[#667eea] to-[#764ba2]`
- **Background Gradient**: `from-blue-50 via-white to-purple-50`
- **Primary Text**: `#1a1a1a`
- **Muted Text**: `#6b7280`
- **Primary Blue**: `#2563eb` (for links)
- **Success Green**: `#10b981` (strong password)
- **Warning Yellow**: `#f97316` (fair password)
- **Error Red**: `#ef4444` (weak password)

### Layout Specifications
- **Card Max Width**: 400px (mobile-friendly)
- **Card Padding**: 24px (1.5rem) on desktop, 16px (1rem) on mobile
- **Input Height**: 40px (2.5rem)
- **Button Height**: 44px (2.75rem) for primary actions
- **Field Spacing**: 16px (1rem) between form fields
- **Section Spacing**: 24px (1.5rem) between sections

### Typography
- **Card Title**: 24px (1.5rem), font-weight: 700
- **Labels**: 14px (0.875rem), font-weight: 500
- **Body Text**: 14px (0.875rem), font-weight: 400
- **Help Text**: 12px (0.75rem), font-weight: 400

### Responsive Breakpoints
- **Mobile**: < 640px (full width cards, smaller padding)
- **Tablet**: 640px - 1024px (max-width cards, centered)
- **Desktop**: > 1024px (max-width cards, centered)

---

## Component Requirements

### Shadcn/ui Components Used
- ✅ `Card` - Container for auth forms
- ✅ `Button` - Form submissions and actions
- 🔲 `Input` - Form text inputs (needs installation)
- 🔲 `Label` - Form field labels (needs installation)
- 🔲 `Checkbox` - Remember me & terms (needs installation)
- ✅ `Progress` - Password strength indicator

### Custom Components Created
- `AuthLayout` - Wrapper for consistent auth page styling
- Password visibility toggle button
- Password strength indicator
- Social login button (disabled state)

### Icons Required (Lucide React)
- `Eye` / `EyeOff` - Password visibility toggle
- `ArrowLeft` - Back navigation
- `Lock` - Password reset icon
- Google SVG icon (included inline)

---

## Content Requirements

### Greek Text
- **Login Page**: "Καλώς ήρθατε!" (Welcome!)
- **Subtitle**: "Welcome back! Sign in to continue learning Greek"

### Form Labels
- **Login**: Email, Password, Remember me
- **Register**: Full Name, Email, Password, Confirm Password
- **Terms**: "I agree to the terms and conditions"

### Button Text
- **Primary Actions**: "Sign In", "Create Account"
- **Secondary**: "Google (Coming Soon)", "Back to Login"
- **Links**: "Forgot password?", "Sign up for free", "Already have an account? Sign in"

### Error/Help Messages (Placeholders)
- Password strength: Weak / Fair / Strong
- Phase 2 notice for password reset
- Support contact message

---

## Visual Requirements

### Page Layout
1. **Full-screen height** with vertical centering
2. **Gradient background** from blue-50 to purple-50
3. **White card** with shadow-xl for elevation
4. **Centered content** with max-width constraint

### Form Design
1. **Stacked layout** with labels above inputs
2. **Full-width inputs** with proper height for touch
3. **Password toggle** button inside input field
4. **Checkbox alignment** with label on same line
5. **Primary button** with gradient background

### Visual Hierarchy
1. **Emoji/Icon** at top for visual interest
2. **Title** as main heading (largest text)
3. **Description** as supporting text (muted color)
4. **Form fields** with clear labels
5. **Primary action** button most prominent
6. **Secondary options** de-emphasized

### Interactive States
- **Hover**: Buttons show opacity change
- **Focus**: Inputs show border color change
- **Disabled**: Google button with reduced opacity
- **Active**: Password visibility toggle changes icon

---

## Success Criteria

### Visual Completeness
- ✅ All three authentication pages created
- ✅ Consistent design across all pages
- ✅ Proper use of Style Guide colors and typography
- ✅ All specified UI elements present
- ✅ Password strength indicator displays correctly

### Layout & Responsiveness
- ✅ Pages centered properly on all screen sizes
- ✅ Cards maintain max-width on large screens
- ✅ Mobile layout works (320px minimum)
- ✅ Tablet layout works (640px - 1024px)
- ✅ Desktop layout works (> 1024px)

### Code Quality
- ✅ TypeScript interfaces for all props
- ✅ Reusable AuthLayout component
- ✅ Clean component structure
- ✅ Proper file organization in pages/auth
- ✅ No TypeScript errors

### User Experience
- ✅ Password visibility toggle works
- ✅ All links have proper hover states
- ✅ Form fields have proper placeholders
- ✅ Google button shows disabled state
- ✅ Greek text displays correctly

### Technical Requirements
- ✅ Routes configured in App.tsx
- ✅ Navigation between pages works
- ✅ No console errors or warnings
- ✅ Components follow React best practices
- ✅ Ready for functional implementation (03.03, 03.04)

---

## Time Breakdown

### Estimated Time: 45 minutes

1. **Install Shadcn Components** - 5 minutes
   - Input, Label, Checkbox components

2. **Create AuthLayout Wrapper** - 5 minutes
   - Shared layout for all auth pages

3. **Design Login Page** - 10 minutes
   - Complete UI with all elements

4. **Design Registration Page** - 10 minutes
   - Include password strength indicator

5. **Create Password Reset Placeholder** - 5 minutes
   - Simple coming soon page

6. **Update Routes** - 5 minutes
   - Add to App.tsx router

7. **Test All Pages** - 5 minutes
   - Verify responsive design
   - Check all visual elements

---

## Important Guidelines

### This is UI/Design Only
- **No validation logic** - Just visual components
- **No form submission** - Use `e.preventDefault()`
- **No state management** - Only local UI state for toggles
- **No API calls** - Pure UI components
- **No authentication logic** - That's task 03.03/03.04

### Focus Areas
1. **Visual design** matching Style Guide
2. **Layout structure** for all screen sizes
3. **Component organization** for reusability
4. **Accessibility** with proper labels and ARIA
5. **User experience** with intuitive design

### Do Not Implement
- Form validation (task 03.03, 03.04)
- Submit handlers with logic (task 03.03, 03.04)
- Auth state management (task 03.02)
- Protected routes (task 03.05)
- Session management (task 03.07)
- Backend integration (future phase)

---

## Executor Instructions

### Quick Start Commands

```bash
# 1. Navigate to frontend directory
cd /Users/samosipov/Downloads/learn-greek-easy/learn-greek-easy-frontend

# 2. Install required Shadcn components
npx shadcn-ui@latest add input label checkbox

# 3. Create directories
mkdir -p src/pages/auth
mkdir -p src/components/auth

# 4. Create the files following the specifications above

# 5. Start dev server to test
npm run dev

# 6. Visit pages
# http://localhost:5173/login
# http://localhost:5173/register
# http://localhost:5173/forgot-password
```

### File Creation Order
1. `src/components/auth/AuthLayout.tsx` - Shared wrapper
2. `src/pages/auth/Login.tsx` - Login page
3. `src/pages/auth/Register.tsx` - Registration page
4. `src/pages/auth/ForgotPassword.tsx` - Reset placeholder
5. Update `src/App.tsx` - Add routes

### Testing Checklist
- [ ] All pages load without errors
- [ ] Responsive design works on mobile
- [ ] Password visibility toggles work
- [ ] Password strength indicator updates
- [ ] Links navigate correctly
- [ ] Hover states display properly
- [ ] Greek text renders correctly
- [ ] Cards are properly centered
- [ ] Google button shows disabled state

---

## Next Steps

After completing this UI design task:

1. **Task 03.02**: Implement Authentication State Management (Zustand)
2. **Task 03.03**: Add login functionality with validation
3. **Task 03.04**: Add registration functionality with validation
4. **Task 03.05**: Implement protected routes

The UI created in this task provides the foundation for all subsequent authentication implementation tasks.

---

**Task Created**: 2025-10-28
**Documentation Version**: 1.0.0
**Last Updated**: 2025-10-28
**Status**: Ready for Implementation
